{
    "comandos": {
        "ajustes": {
            "prefix": {
                "variable1": "❌ You have to specify the new prefix for the Bot!",
                "variable2": "`✅ Changed the Prefix from \\`${prefix}\\` to \\`${args[0]}\\``"
            },
            "set-language": {
                "variable1": "`❌ You have to specify the new language for the Bot!\n**Available Languages**: ${idiomas.map(idioma => `\\`${idioma}\\``).join(', ')}`",
                "variable2": "`❌ You have to specify a valid language for the Bot!\n**Available Languages**: ${idiomas.map(idioma => `\\`${idioma}\\``).join(', ')}`",
                "variable3": "`✅ Changed the language from \\`${idioma}\\` to \\`${args[0]}\\``"

            },

            "claim": {
                "variable1": "❌ **The key you mentioned has already been used!**",
                "variable2": "`✅ **Premium features have been activated on this server!**\nExpire In <t:${Math.round((Date.now() + Number(clave.duracion)) / 1000)}:R>`",
                "variable3": "❌ **The key you entered was not found!**"
            }
        }
    }
}